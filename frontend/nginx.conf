server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Route /about -> about.html
    location = /about {
        try_files /about.html =404;
    }

    # Mọi route khác fallback về index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
